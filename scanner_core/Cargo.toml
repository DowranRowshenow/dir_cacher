[package]
name = "scanner_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "scanner_core"
crate-type = ["cdylib"]   # produces scanner_core.dll on Windows

# Zero external dependencies — pure Rust stdlib only
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
